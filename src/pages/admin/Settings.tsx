import { AdminPlaceholder } from '../../components/admin/AdminPlaceholder'

export default function AdminSettings() {
  return <AdminPlaceholder title="Admin Settings" description="Configure risk thresholds, alert rules, and platform preferences." />
}