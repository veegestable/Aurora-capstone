{
  "name": "aurora-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "build:only": "npm run build",
    "deploy": "npm run build",
    "deploy:firebase": "npm run build && cd .. && firebase deploy --only functions",
    "deploy:messaging": "npm run build && cd .. && firebase deploy --only functions:sendTextMessageTrusted,functions:sendSessionRequestTrusted"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
