/**
 * Must match floating tab bar geometry in app/(student)/_layout.tsx.
 * Used for scene padding so content is not covered by the absolute tab bar.
 */
export const STUDENT_TAB_BAR_FLOAT_BOTTOM = 20;
export const STUDENT_TAB_BAR_HEIGHT = 72;
/** Extra gap between content and the top of the tab bar */
export const STUDENT_TAB_BAR_CONTENT_GAP = 12;

export const STUDENT_TAB_BAR_BOTTOM_CLEARANCE =
    85;
